name: Custom Workflow

on:
  push:
    branches: [ "main" ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: 21f2000304@ds.study.iitm.ac.in
        run: echo "Hello, World!"
